import { LoggerInterface } from './StatsigOptions';
export default abstract class OutputLogger {
    static getLogger(): LoggerInterface;
    static setLogger(logger: LoggerInterface): void;
    static resetLogger(): void;
}
